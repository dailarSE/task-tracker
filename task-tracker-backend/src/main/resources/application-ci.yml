spring:
  datasource:
    username: devuser
    password: devpass
logging:
  level:
    com.example.tasktracker.backend: DEBUG
    org.springframework: INFO
    org.hibernate: WARN

otel:
  sdk:
    disabled: true

  endpoints:
    web:
      exposure:
        include: health
